name: mewtionary_teacher3d_bridge
description: Flutter to embedded Unity bridge for Mewtionary 3D Teacher.
version: 2.2.0
homepage: https://example.invalid/mewtionary

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.astratechnologies.mewtionary_teacher3d
        pluginClass: MewtionaryTeacher3DPlugin
